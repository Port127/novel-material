---
description: Prefer python command over python3
alwaysApply: true
---

# Python Command Preference

- Use `python` for Python commands by default.
- Do not use `python3` unless the user explicitly requests it.
- If a script or environment requires a specific interpreter path, explain why and ask before changing command style.
