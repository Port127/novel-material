# Novel Material V2 - Embedding 模型配置
embedding:
  provider: openai  # openai / bge / local
  model: text-embedding-3-small
  dimension: 1024
  api_key: "sk-xxx"
  base_url: "https://api.openai.com/v1"

  # 可选：本地 BGE 模型（中文优化）
  # model: "BAAI/bge-large-zh-v1.5"
  # dimension: 1024
  # device: "cpu"
